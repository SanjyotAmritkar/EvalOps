# EvalOps environment configuration -- TEMPLATE ONLY.
#
# None of these variables are read by any code yet. Provider integrations and
# configuration loading arrive in Phase 1 (see docs/ARCHITECTURE.md).
#
# Usage: copy this file to .env and fill in values as later phases require.
# .env is git-ignored; never commit real secrets.

# --- Hosted model providers ---
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# --- Local model provider ---
OLLAMA_BASE_URL=http://localhost:11434
