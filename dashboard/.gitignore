# dependencies
node_modules/
.pnp
.pnp.*

# next / build
.next/
out/
build/
*.tsbuildinfo
# regenerated by `next typegen` (our typecheck script runs it) and `next dev`;
# its content differs between the two, so keep it untracked
next-env.d.ts

# test / coverage
coverage/

# env
.env
.env*.local

# misc
.DS_Store
*.log

# next dev auto-scaffolds these; the repo's guidance is in the root CLAUDE.md
AGENTS.md
CLAUDE.md
